import { Group } from "react-native";

export default class R {
    static image = {
        newImage: require('../image/Group.png'),
    };
    static string = {
        newText: 'Hello, world!',
    };

};